{
  "name": "lights-area-strategy",
  "version": "1.1.0",
  "description": "Home Assistant Dashboard-Strategie: Lampen auf der Startseite, Bereichskarten als Einstieg in automatisch generierte Detailansichten.",
  "type": "module",
  "license": "MIT",
  "scripts": {
    "build": "npm run typecheck && node build.mjs",
    "watch": "node build.mjs --watch",
    "typecheck": "tsc --noEmit"
  },
  "devDependencies": {
    "esbuild": "^0.23.1",
    "typescript": "^5.6.2"
  }
}
